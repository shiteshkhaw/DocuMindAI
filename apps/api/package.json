{
  "name": "api-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "venv\\Scripts\\python -m uvicorn main:app --host 127.0.0.1 --port 8000 --reload",
    "build": "echo \"No build step for Python API\"",
    "test": "venv\\Scripts\\python -m pytest"
  }
}
